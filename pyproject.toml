[project]
name = "masim-analysis"
version = "0.1.0"
description = "Anaylsis tools for the Temple malaria simulation model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter",
    "matplotlib",
    "pandas",
]
