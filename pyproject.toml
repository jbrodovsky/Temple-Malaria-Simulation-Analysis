[project]
name = "temple-malaria-simulation-analysis"
version = "0.1.0"
description = "Anaylsis tools for the Temple malaria simulation model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.0",
    "pandas>=2.2.3",
]
